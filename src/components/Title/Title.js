export default function Title ({greeting}) {
    return (
        <h1 style={{color:`blue`, fontsize:`100px`}}>{greeting}</h1>
    )
}
/* lo de {texto} es para que pueda poner distintos titulos segun la pagina */